#include <stdio.h>

/**
	* main - Entry point
	* Description: 'Basic Uses of If else statements'
	* Return: Always 0 (Success)
*/

int main(void)
{
	int counter;

	for (counter = 0; counter < 10; counter++)
	{
		printf("%d", counter);
	}
	printf("\n");
	return (0);
}
