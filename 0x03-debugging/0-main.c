#include "main.h"
#include <stdio.h>

/**
 * main - Tests the positive_or_negative function with input 0
 *
 * Return: Always 0
 */
int main(void)
{
	int i = 0;

	printf("Testing positive_or_negative() function with input 0:\n");
	positive_or_negative(i);

	return (0);
}

